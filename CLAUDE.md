# CLAUDE.md

Repo context for Claude Code lives in **`.claude/handoff.md`** (gitignored, local-only).

Read it before scanning the repo. It covers:
- What this repo is and the three-repo ecosystem
- Top-level layout (`foundations/`, `workflows/`, `patterns/`, `composition/`, `meta/`, `website/`)
- The 3-tier documentation convention (overview / design / implementation + evolution.md)
- Website stack, build commands, and source map
- Common tasks → where to edit
- Gotchas (no markdown duplication into the site, frontmatter validation, README ↔ `patterns.ts` sync)

If `.claude/handoff.md` is missing (fresh clone), ask the user before regenerating — the prior version may have local notes.
